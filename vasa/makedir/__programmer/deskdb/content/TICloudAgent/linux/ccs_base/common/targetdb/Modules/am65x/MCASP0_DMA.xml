<module name="MCASP0_DMA" acronym="" XML_version="1.0" HW_revision="n/a" description="">
  <register id="MCASP_RXBUF" acronym="MCASP_RXBUF" offset="0x8000" width="32" description="Through the DATA port , the Host can service all serializers through a single address and the MCASP automatically cycles through the appropriate serializers. For receive operations through the DATA port, the Host should read from the same MCASP_RBUF DATA port address to service all of the active receive serializers upon each receive data ready event. To enable accesses from the Host to the MCASP_RXBUFn registers through the DATA port, one must clear the RBUSEL bits to 0 in the respective registers.">
    <bitfield id="RXBUF" width="32" begin="31" end="0" resetval="0x0" description="Rx buffer data." range="" rwaccess="R"/>
  </register>
  <register id="MCASP_TXBUF" acronym="MCASP_TXBUF" offset="0x8000" width="32" description="Through the DATA port, the Host can service all serializers through a single address and the MCASP automatically cycles through the appropriate serializers. For transmit operations through the DATA port, the Host should write to the same DATA port address to service all of the active transmit serializers upon each transmit data ready event. To enable accesses from the Host to the MCASP_TXBUFn registers through the DATA port, one must clear the XBUSEL bits to 0 in the respective registers.">
    <bitfield id="TXBUF" width="32" begin="31" end="0" resetval="0x0" description="Tx buffer data." range="" rwaccess="W"/>
  </register>
</module>
