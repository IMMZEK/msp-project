<module name="PRU_ICSSG0_PR1_PDSP0_IRAM_DEBUG" acronym="" XML_version="1.0" HW_revision="n/a" description="">
  <register id="ICSSG_DBG_GPREG_y" acronym="ICSSG_DBG_GPREG_y" offset="0x0" page = "2" width="32" description="DEBUG PRU General Purpose Registers 0 to 31. This register allows an external agent to debug the PRU while it is disabled. Reading or writing to these registers will have the same effect as a read or write to these registers from an internal instruction in the PRU. For R30, this includes generation of the pulse outputs whenever the register is written. Offset = 0h + (y * 4); where y = 0 to 31">
    <bitfield id="GP_REGy" width="32" begin="31" end="0" resetval="0x0" description="PRU Internal GP Register y: Reading / writing this field directly inspects/modifies the corresponding internal register in the PRU internal register file." range="" rwaccess="RW"/>
  </register>
  <register id="ICSSG_DBG_CT_REG_y" acronym="ICSSG_DBG_CT_REG_y" offset="0x80" page = "2" width="32" description="DEBUG PRU Constant Table Entry Registers 0 to 31. This register allows an external agent to debug/or configure the PRU while it is disabled. Since some of the constants table entries may actually depend on system inputs / and or the internal state of the PRU, these registers are provided to allow an external agent to easily determine the state of the constants table. Offset = 80h + (y * 4); where y = 0 to 31">
    <bitfield id="CT_REGy" width="32" begin="31" end="0" resetval="0x0" description="PRU Internal Constants Table Entry y (where y = 0 to 31): Reading this field directly inspects the corresponding entry in the PRU internal constants table. Note: For each register y where y is less than the ctreg_cnt configuration parameter, the PRU internal Constants Table Entry Register will be writable and these registers are to be used to set the value for these constants. As for reads, the PRU must be disabled in order to access these registers." range="" rwaccess="RW"/>
  </register>
</module>
