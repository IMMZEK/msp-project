/*******************************************************************/
/* KS3 DEBUG ConnectIfDisconnected                                 */
/*******************************************************************/


/* \brief Connect to target if not already connected. */
ConnectIfDisconnected()
{
    if(GEL_IsConnected() == 0)
    {
        GEL_Connect();
    }
}
