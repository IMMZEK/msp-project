import { HandoffManager } from '../handoff/handoff-manager';
import { RexObject } from '../lib/rex';
export declare function getHandoffManagerForTesting(): HandoffManager;
export declare function getRoutes(rex: RexObject): import("express-serve-static-core").Router;
