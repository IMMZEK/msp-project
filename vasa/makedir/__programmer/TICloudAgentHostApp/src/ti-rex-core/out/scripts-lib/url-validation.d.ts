export const command: string;
export const describe: "Run url validation on desktop without dinfra";
export namespace builder {
    namespace contentPath {
        let describe: string;
        let demandOption: boolean;
    }
}
export function handler(argv: any): void;
